apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: function-generate-video
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: limelapse.com/function-generate-video:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: QUARKUS_DATASOURCE_DB_KIND
              value: "postgresql"
            - name: QUARKUS_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: QUARKUS_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB # redundancy for next env var injection
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_DB
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: "jdbc:postgresql://postgres:5432/$(POSTGRES_DB)"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: function-generate-video-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/preserve-host: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    cert-manager.io/cluster-issuer: local-ca-issuer
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Forwarded-Proto $pass_access_scheme;
      proxy_set_header X-Forwarded-Port $pass_port;
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - function-generate-video.limelapse.com
      secretName: function-generate-video-tls
  rules:
    - host: function-generate-video.limelapse.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: knative-bridge
                port:
                  number: 80
